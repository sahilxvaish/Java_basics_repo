public class operator_and_binary_number_sys {
    public static void main(String[] args) {
        //operators
        //symbols that tell compiler to perform some operation.

        //Arithmetic operators
        //binary [+, -, *, /, % (modular) modular gives remainder]
        //unary[++, --]

        //Relational operators
        //[==, !=(not equal), >, <, >=, <=]

        //logical operators
        //[&&(AND), ||(OR), !(NOT)]

        //BINARY NUMBER SYSTEM
        //bitwise operators
        //[&, |, ^(binary XOR), ~(binary one"s compliment), <<(left shift), >>(right shift)]

        //Assignment operators
        //[=, +=, -=, *=, /=]
    }
}
